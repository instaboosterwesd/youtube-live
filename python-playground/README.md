# Python Playground

A clean, dependency-free Python project for simple scripts and experiments.

## Run the example

From the workspace root:

```sh
python python-playground/main.py
```

Or from this folder:

```sh
cd python-playground
python main.py
```

Edit `main.py` or add more `.py` files as you experiment.